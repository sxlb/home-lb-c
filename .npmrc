# 国内镜像加速：VPS 位于中国，直连 npmjs.org 极慢/超时导致构建卡死。
# npm 10 的 replace-registry-host=lockfile 默认会把 lockfile 中的 registry.npmjs.org
# 改写为下方 registry，因此 npm ci 的依赖解析仍与 lockfile 保持一致。
registry=https://registry.npmmirror.com