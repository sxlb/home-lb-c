# 更新日志

## [v0.0.4] - 2026-09-09

### ✨ 新增功能

**1. 命令面板（Ctrl/Cmd+K 或 `/` 唤起）**
- 全局快捷键唤起命令面板，支持 Esc 关闭、输入框内不劫持快捷键
- 搜索网站 / 友链并回车快捷跳转，箭头键键盘导航
- 分组图标区分网站与友链结果，无结果空态、`noopener,noreferrer` 安全跳转
- 面板音乐入口打通音乐播放列表

**2. 作品集 + 技能云**
- 新增 `Project` / `Skill` 数据模型与迁移，后台面板管理（封面、置顶、标签、熟练度、图标）
- 前台技能云展示（TypeScript / 熟练度渐变进度条），并入首屏左栏
- 作品与网站、友链共用一个多 Tab 导航容器（无数据 tab 自动隐藏）

**3. 随笔 / 文章阅读**
- 新增 `Article` 模型（Markdown 正文、摘要、封面、标签、发布状态、阅读量、置顶）
- 前台文章列表页 + 阅读页，`react-markdown` + GFM 渲染，阅读量计数
- 站点地图收录文章 URL，Open Graph 社交卡片元信息
- 后台随笔管理面板（Markdown 编辑、发布状态控制、slug 规则校验）

**4. 访客地域统计 + 导出**
- 新增 `/api/stats/geo` 地域聚合（ip2region 离线解析，省份 / 国家 TOP 排行）
- CSV 导出（含逗号 / 引号转义，BOM 标识），后台访问统计面板展示

### 🚀 性能优化

- **后台首屏提速**：16 个面板全部 `React.lazy` 懒加载按需分包，`/admin` 首屏 JS 降至 ~137KB
- **后台切换流畅**：面板保活缓存（切回保留表单 / 滚动 / 状态，零重载）+ 骨架屏
- **侧边栏渲染优化**：`NavItem` / `BrandHeader` 提为模块级 memo，消除整树重挂载
- **骨架屏去重**：`AdminPage` 复用 `profileShared.loadProfile` 的 inflight 去重，消除重复 GET `/api/profile`
- **统计请求合并**：`/api/stats` POST 一次请求同时完成上报与读取，省一次往返
- **访客位置预取提前**：欢迎弹窗组件挂载即并行获取访客信息 + 公告，消除弹窗展示等待
- **首页 SSR 并行**：`getHomeData` 用 `Promise.all` 并行查询 6 张关联表 + 头像 + 壁纸

### 🐛 Bug 修复

- **卡片 hover 位移**：链接卡片悬停时异常上浮/放大，已覆盖抑制
- **Tab 切换高度跳动**：链接容器固定最小高度，网站 / 友链 / 作品切换不再跳变
- **进度条拖拽残留**：按住拖出进度条区域后 `draggingRef` 未复位，加全局 `pointerup` 兜底
- **第三方依赖消除**：欢迎弹窗直连第三方 `v4.yinghualuo.cn` 定位改为自研 `/api/visitor/location`（复用 ip2region）
- **命令面板死字段**：清理未使用的 `CmdLink.icon` 字段及多余 `.map()`
- **SSR 崩溃**：`SkillCloud` 补 `"use client"` 指令，修复服务端调用客户端图标解析方法的边界错误

### ♻️ 代码整理

- 抽取 `lib/article.ts`（文章数据白名单映射 + 摘要自动生成）与 `lib/upsert.ts`（批量保存事务封装），消除重复代码
- 删除孤儿组件 `PortfolioSection.tsx` 与无消费者接口 `/api/articles/public`
- `prisma/seed.js` 清理未使用变量；全库 ESLint 零告警

### 🧪 测试

- 新增 `tests/article.test.ts` 4 项用例（`autoExcerpt` 等文章共享逻辑），锁定行为防回归
- 全量 **35 个测试文件 / 280 项用例** 全部通过

### 🗑️ 数据清理

- 移除残留测试数据：友链 `12232`、网站链接 `测试` / `4525325` / `145325` / `1354354` / `14545453` 及关联点击统计
- 现存测试（GitHub 页面误配为图标，触发 `ERR_BLOCKED_BY_ORB`）随数据删除一并消除

