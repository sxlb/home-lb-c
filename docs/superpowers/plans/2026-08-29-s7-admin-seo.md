# S7 日志/页脚/SEO 实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 操作日志筛选/搜索/分页，页脚自定义 HTML，sitemap/robots 自动生成。

**Architecture:** 日志 API 增加 module/keyword/page/pageSize 并返回 total；页脚新字段 siteFooterHtml 走既有 Profile 字段链路（schema→validation→shared→route→panel→hooks→Footer）；sitemap/robots 为 Next.js App Router 元数据文件读 Profile 配置。

**Tech Stack:** Next.js 15 / Prisma 5 + SQLite / zod / vitest

**设计文档:** `docs/superpowers/specs/2026-08-29-s7-admin-seo-design.md`

---

### Task 1: 操作日志 API 筛选/搜索/分页

**Files:**
- Modify: `app/api/operation-logs/route.ts`

- [ ] **Step 1: 重写 GET 逻辑**

将 `app/api/operation-logs/route.ts` 的 GET 函数替换为：

```ts
import { NextResponse, NextRequest } from "next/server";
import { prisma } from "@/lib/db";
import { internalError, error, requireSession } from "@/lib/server";

export const dynamic = "force-dynamic";

// 查询操作日志（仅后台管理员可用）
// 参数：module=模块筛选 / keyword=关键词（username/summary 模糊匹配）/ page=页码（默认1）/ pageSize=每页条数（默认20，最大100）
export async function GET(request: NextRequest) {
  try {
    const session = await requireSession();
    if (!session) {
      return error("未授权", 401);
    }

    const sp = request.nextUrl.searchParams;
    const module = sp.get("module") || undefined;
    const keyword = sp.get("keyword")?.trim() || undefined;

    const rawPage = parseInt(sp.get("page") || "1", 10);
    const page = isNaN(rawPage) || rawPage < 1 ? 1 : rawPage;
    const rawPageSize = parseInt(sp.get("pageSize") || "20", 10);
    const pageSize = isNaN(rawPageSize) ? 20 : Math.min(Math.max(rawPageSize, 1), 100);

    const where = {
      ...(module ? { module } : {}),
      ...(keyword
        ? { OR: [{ username: { contains: keyword } }, { summary: { contains: keyword } }] }
        : {}),
    };

    const [total, items] = await Promise.all([
      prisma.operationLog.count({ where }),
      prisma.operationLog.findMany({
        where,
        orderBy: { id: "desc" },
        skip: (page - 1) * pageSize,
        take: pageSize,
      }),
    ]);

    return NextResponse.json({ items, total, page, pageSize });
  } catch (e) {
    return internalError("[GET /api/operation-logs] 查询失败", e);
  }
}
```

- [ ] **Step 2: 类型检查**

Run: `npx tsc --noEmit`
Expected: 无报错

- [ ] **Step 3: 提交**

```bash
git add app/api/operation-logs/route.ts
git commit -m "feat(logs): add module filter, keyword search and pagination to operation logs API"
```

---

### Task 2: 操作日志面板筛选与分页 UI

**Files:**
- Modify: `components/admin/OperationLogPanel.tsx`

- [ ] **Step 1: 更新模块标签常量**

在 `MODULE_LABEL` / `MODULE_COLOR` / `ACTION_LABEL` / `ACTION_COLOR` 中补充：

```ts
// MODULE_LABEL 增加：
  "friend-links": "友情链接",
  backup: "数据管理",
// MODULE_COLOR 增加：
  "friend-links": "bg-teal-500/15 text-teal-600 dark:text-teal-400",
  backup: "bg-rose-500/15 text-rose-600 dark:text-rose-400",
// ACTION_LABEL 增加：
  restore: "恢复",
// ACTION_COLOR 增加：
  restore: "text-rose-600 dark:text-rose-400",
```

- [ ] **Step 2: 面板改为受控筛选 + 分页**

将组件主体重构：新增 `module`、`keyword`、`page` state；`load()` 带参请求；渲染筛选行与分页栏。

```tsx
export default function OperationLogPanel() {
  const [logs, setLogs] = useState<OperationLog[]>([]);
  const [total, setTotal] = useState(0);
  const [page, setPage] = useState(1);
  const [module, setModule] = useState("");
  const [keyword, setKeyword] = useState("");
  const [loading, setLoading] = useState(true);
  const [expandedId, setExpandedId] = useState<number | null>(null);
  const seqRef = useRef(0);
  const mountedRef = useRef(true);

  const PAGE_SIZE = 20;

  useEffect(() => {
    mountedRef.current = true;
    return () => {
      mountedRef.current = false;
    };
  }, []);

  async function load(p = page, m = module, k = keyword) {
    const seq = ++seqRef.current;
    setLoading(true);
    try {
      const params = new URLSearchParams();
      if (m) params.set("module", m);
      if (k.trim()) params.set("keyword", k.trim());
      params.set("page", String(p));
      params.set("pageSize", String(PAGE_SIZE));
      const res = await fetch(`/api/operation-logs?${params.toString()}`);
      if (!mountedRef.current || seq !== seqRef.current) return;
      if (res.ok) {
        const data = await res.json();
        setLogs(data.items);
        setTotal(data.total);
        setPage(data.page);
      } else toast.error("加载日志失败");
    } catch {
      if (mountedRef.current && seq === seqRef.current) toast.error("网络错误");
    } finally {
      if (mountedRef.current && seq === seqRef.current) setLoading(false);
    }
  }

  useEffect(() => {
    load(1, "", "");
    // eslint-disable-next-line react-hooks/exhaustive-deps
  }, []);

  const totalPages = Math.max(1, Math.ceil(total / PAGE_SIZE));

  const applyFilter = () => {
    setPage(1);
    load(1, module, keyword);
  };
```

筛选行（CardHeader 下方）与分页栏（列表底部）JSX：

```tsx
      <CardContent>
        {/* 筛选行 */}
        <div className="mb-3 flex flex-wrap items-center gap-2">
          <select
            value={module}
            onChange={(e) => setModule(e.target.value)}
            className="h-8 rounded-md border border-input bg-background px-2 text-sm"
          >
            <option value="">全部模块</option>
            {Object.entries(MODULE_LABEL).map(([key, label]) => (
              <option key={key} value={key}>{label}</option>
            ))}
          </select>
          <input
            value={keyword}
            onChange={(e) => setKeyword(e.target.value)}
            onKeyDown={(e) => e.key === "Enter" && applyFilter()}
            placeholder="搜索操作人或摘要"
            className="h-8 w-48 rounded-md border border-input bg-background px-3 text-sm"
          />
          <Button size="sm" variant="outline" onClick={applyFilter} className="gap-1.5">
            <RefreshCw className="h-4 w-4" />
            筛选
          </Button>
        </div>

        {/* 列表渲染（原 loading / empty / list 逻辑保留，list.map 不变） */}
        ...
        {/* 分页栏 */}
        {total > 0 && (
          <div className="mt-4 flex items-center justify-between text-xs text-muted-foreground">
            <span>共 {total} 条记录</span>
            <div className="flex items-center gap-2">
              <Button size="sm" variant="outline" disabled={page <= 1 || loading} onClick={() => { const p = page - 1; setPage(p); load(p, module, keyword); }}>
                上一页
              </Button>
              <span>{page} / {totalPages}</span>
              <Button size="sm" variant="outline" disabled={page >= totalPages || loading} onClick={() => { const p = page + 1; setPage(p); load(p, module, keyword); }}>
                下一页
              </Button>
            </div>
          </div>
        )}
```

- [ ] **Step 3: 类型检查 + 构建**

Run: `npx tsc --noEmit && npm run build`
Expected: 成功

- [ ] **Step 4: 提交**

```bash
git add components/admin/OperationLogPanel.tsx
git commit -m "feat(logs): filter bar and pagination in operation log panel"
```

---

### Task 3: Profile 新增 siteFooterHtml 字段链路

**Files:**
- Modify: `prisma/schema.prisma:70`（updatedAt 前）
- Create: `prisma/migrations/20260829010000_add_site_footer_html/migration.sql`
- Modify: `lib/validation.ts`、`components/admin/profileShared.ts`、`app/api/profile/route.ts`、`app/hooks.ts`

- [ ] **Step 1: schema 增加字段**

在 `prisma/schema.prisma` 的 Profile 模型（`updatedAt` 前）加入：

```prisma
  siteFooterHtml          String   @default("") // 页脚自定义 HTML（管理员可信内容，显示在版权行上方）
```

- [ ] **Step 2: 创建迁移 SQL**

创建 `prisma/migrations/20260829010000_add_site_footer_html/migration.sql`：

```sql
-- AlterTable
ALTER TABLE "Profile" ADD COLUMN "siteFooterHtml" TEXT NOT NULL DEFAULT '';
```

Run: `npx prisma generate`

- [ ] **Step 3: validation.ts 增加字段**

在 `lib/validation.ts` 的 profileSchema 中（`siteMps` 附近）加入：

```ts
  // 页脚自定义 HTML（管理员录入，视为可信内容，与 analyticsScript 同信任级）
  siteFooterHtml: z
    .string()
    .max(20000, "页脚 HTML 过长")
    .optional()
    .default(""),
```

- [ ] **Step 4: profileShared.ts 增加字段**

在 `components/admin/profileShared.ts` 的 ProfileShape 接口与 INITIAL_PROFILE 中分别加入 `siteFooterHtml: string;` 与 `siteFooterHtml: ""`。

- [ ] **Step 5: profile route 透传**

在 `app/api/profile/route.ts` 的解构与 data 对象中分别加入 `siteFooterHtml,`。

- [ ] **Step 6: hooks.ts 透传**

在 `app/hooks.ts` 的返回对象中加入 `siteFooterHtml: profile?.siteFooterHtml || "",`，并在 Promise 返回类型接口中加 `siteFooterHtml: string;`。

- [ ] **Step 7: 类型检查 + 全量测试**

Run: `npx tsc --noEmit && npx vitest run`
Expected: 通过

- [ ] **Step 8: 提交**

```bash
git add prisma/schema.prisma prisma/migrations/20260829010000_add_site_footer_html/migration.sql lib/validation.ts components/admin/profileShared.ts app/api/profile/route.ts app/hooks.ts
git commit -m "feat(footer): add siteFooterHtml field across schema/validation/shared/api/hooks"
```

---

### Task 4: ProfilePanel 页脚 textarea + Footer 渲染

**Files:**
- Modify: `components/admin/ProfilePanel.tsx`（页脚区）
- Modify: `components/Footer.tsx`

- [ ] **Step 1: ProfilePanel 页脚区加入 textarea**

在 `components/admin/ProfilePanel.tsx` 页脚区块（siteStart 字段附近）加入：

```tsx
                  <div>
                    <Label htmlFor="siteFooterHtml">页脚自定义 HTML</Label>
                    <textarea
                      id="siteFooterHtml"
                      name="siteFooterHtml"
                      value={profile.siteFooterHtml}
                      onChange={(e) => set("siteFooterHtml", e.target.value)}
                      placeholder='如 <a href="https://example.com">友链</a>'
                      rows={3}
                      className="mt-1 flex w-full rounded-md border border-input bg-background px-3 py-2 text-sm ring-offset-background focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-ring focus-visible:ring-offset-2"
                    />
                    <p className="mt-1 text-xs text-muted-foreground">
                      显示在页脚版权行上方，支持 HTML 标签（管理员可信内容）
                    </p>
                  </div>
```

同时 ProfilePanel 类型与默认值（Task 3 Step 4 的同名文件已加，但 ProfilePanel 有自己的 Profile 接口与 INITIAL，需同步加 `siteFooterHtml: string;` 与默认 `siteFooterHtml: ""`）。

- [ ] **Step 2: Footer 渲染自定义 HTML**

修改 `components/Footer.tsx`：
1. Props 增加 `siteFooterHtml?: string;`
2. 在 `groups.map` 之后、Copyright 行之前插入：

```tsx
        {siteFooterHtml && (
          <div
            className="text-xs md:text-sm"
            dangerouslySetInnerHTML={{ __html: siteFooterHtml }}
          />
        )}
```

3. 组件开头 `const footerHtml = siteFooterHtml.trim();`，渲染处用 `footerHtml &&`。

- [ ] **Step 3: 更新 Footer 调用处（hooks 透传已在 Task 3，page.tsx 或 Footer 懒加载处）**

`app/page.tsx` 中 `<FooterLazy ... />` 传入 `siteFooterHtml={d.siteFooterHtml}`（确认 Footer 的懒加载 props 透传位置）。

- [ ] **Step 4: 类型检查 + 构建**

Run: `npx tsc --noEmit && npm run build`
Expected: 成功

- [ ] **Step 5: 提交**

```bash
git add components/admin/ProfilePanel.tsx components/Footer.tsx app/page.tsx
git commit -m "feat(footer): render custom footer HTML in ProfilePanel and Footer"
```

---

### Task 5: sitemap / robots

**Files:**
- Create: `app/sitemap.ts`
- Create: `app/robots.ts`

- [ ] **Step 1: 创建 sitemap.ts**

创建 `app/sitemap.ts`：

```ts
import { prisma } from "@/lib/db";
import type { MetadataRoute } from "next";

export const dynamic = "force-dynamic";

/** 站点地图：配置 siteUrl 后输出主页 URL，否则空数组 */
export default async function sitemap(): Promise<MetadataRoute.Sitemap> {
  const profile = await prisma.profile.findFirst({ orderBy: { id: "asc" } });
  const siteUrl = profile?.siteUrl?.trim().replace(/\/+$/, "");
  if (!siteUrl) return [];

  return [
    {
      url: siteUrl,
      lastModified: profile.updatedAt,
      changeFrequency: "daily",
      priority: 1,
    },
  ];
}
```

- [ ] **Step 2: 创建 robots.ts**

创建 `app/robots.ts`：

```ts
import { prisma } from "@/lib/db";
import type { MetadataRoute } from "next";

export const dynamic = "force-dynamic";

/** 爬虫规则：允许全站；配置 siteUrl 后声明 sitemap 地址 */
export default async function robots(): Promise<MetadataRoute.Robots> {
  const profile = await prisma.profile.findFirst({ orderBy: { id: "asc" } });
  const siteUrl = profile?.siteUrl?.trim().replace(/\/+$/, "");
  return {
    rules: [{ userAgent: "*", allow: "/" }],
    ...(siteUrl ? { sitemap: `${siteUrl}/sitemap.xml` } : {}),
  };
}
```

- [ ] **Step 3: 类型检查 + 构建**

Run: `npx tsc --noEmit && npm run build`
Expected: 成功

- [ ] **Step 4: 提交**

```bash
git add app/sitemap.ts app/robots.ts
git commit -m "feat(seo): generate sitemap and robots from siteUrl config"
```

---

### Task 6: 全量验证

- [ ] **Step 1: 全量测试**

Run: `npx vitest run`
Expected: 全部 PASS

- [ ] **Step 2: 类型 + Lint + 构建**

Run: `npx tsc --noEmit && npm run lint && npm run build`
Expected: 无错误

- [ ] **Step 3: 手工验证清单**

1. 后台「操作日志」：模块下拉筛选（如数据管理）、关键词搜索、上一页/下一页与总数显示正确
2. 站点信息 → 页脚区：填自定义 HTML（如 `<a href="https://example.com">友链</a>`）保存 → 首页页脚版权行上方显示
3. `siteUrl` 配置后访问 `/sitemap.xml` 返回主页 URL、`/robots.txt` 含 sitemap 声明；未配置时 sitemap 为空数组
4. 未登录访问 `/api/operation-logs` 返回 401

- [ ] **Step 4: 最终提交**

```bash
git add -A
git commit -m "chore(s7): final verification" || echo "无新增变更"
```
